namespace Chess
{
    class Program
    {
        static void Main(string[] args)
        {
            // Piece testPiece = new Piece("bishop", "white", "a2");
            // Board testBoard = new Board();

            // Pawn testPawn = new Pawn("a2", "white");
            // Console.WriteLine(testPawn.ToString());
            // testPawn.Move(0);
            // Console.WriteLine(testPawn.ToString());

            // Bishop testBishop = new Bishop("c1", "white");
            // Console.WriteLine(testBishop.ToString());
            // testBishop.Move(1, 3);
            // Console.WriteLine(testBishop.ToString());

            // Board testBoard = new Board();
            // Console.WriteLine(testBoard.ToString());
            // Console.Write("Enter a move (e.g. a2-a3): ");
            // string move = (string)Console.ReadLine();
            // string move = "a2-a3";
            // testBoard.MovePiece(move);
            // Console.WriteLine(testBoard.ToString());

            Bishop b = new Bishop("white", "g2");
        }
    }
}